<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Fasty App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>
    <!-- ★ これが無いと Stimulus/Turbo が動きません -->
    <%= javascript_import_module_tag "application" %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="text-gray-900 min-h-screen flex flex-col">
    <% landing = (controller_path == "pages" && action_name == "home") %>

    <!-- ヘッダーは常に表示 -->
    <%= render "shared/header" %>

    <% if landing %>
      <!-- ランディング：ヒーローを上に詰めるためのクラスを付与 -->
      <main class="grow landing-root">
        <!-- フラッシュを出す場合はヒーローを少し引き上げたいときに使える -->
        <div class="pull-up-after-flash">
          <%= render "shared/flash" %>
        </div>
        <%= yield %>
      </main>
    <% else %>
      <main class="grow max-w-5xl mx-auto p-4 pt-4">
        <%= render "shared/flash" %>
        <%= yield %>
      </main>
    <% end %>

    <%= render "shared/footer" %>
  </body>
</html>
