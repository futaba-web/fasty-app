<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Fasty" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# --- CSS: Tailwind + application（@importで自作CSSを束ねる） --- %>
    <%= stylesheet_link_tag "tailwind", "application", "layout", "drawer", "landing", "devise", "data-turbo-track": "reload" %>

    <%# 画面個別で <head> 追記したい時に使う %>
    <%= yield :head %>

    <%# --- JS (importmap) --- %>
    <%= javascript_importmap_tags %>

    <%# --- PWA / Icons --- %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
  </head>

  <%# ===== 背景画像の切替ロジック ===== %>
  <% landing       = (controller_path == "pages" && action_name == "home") %>  <%# 未ログインのトップ %>
  <% default_bg    = asset_path("landing/hero.jpg") %>                          <%# 未ログイン用 %>
  <% authed_bg     = asset_path("landing/hero-authed.png") %>                   <%# ログイン後用 %>
  <% page_override = content_for?(:hero_bg_url) ? yield(:hero_bg_url) : nil %>  <%# 個別指定があれば最優先 %>
  <% auth_page     = (respond_to?(:devise_controller?) && devise_controller?) %><%# Devise 画面 %>

  <% show_hero = landing || auth_page || user_signed_in? || page_override.present? %>
  <% bg_url    = page_override || (user_signed_in? ? authed_bg : default_bg) %>

  <body
    data-controller="menu"
    class="min-h-dvh flex flex-col text-gray-900<%= landing ? ' landing-root' : '' %><%= show_hero ? ' hero-bg' : '' %>"
    style="<%= show_hero ? "--hero-bg: url('#{bg_url}')" : '' %>">

    <%= render "shared/header" %>

    <%# ログイン後のみドロワー %>
    <%= render("shared/menu_drawer") if user_signed_in? %>

    <main class="grow">
      <div class="mx-auto max-w-5xl px-4">
        <%= render "shared/flash" %>
      </div>
      <%= yield %>
    </main>

    <footer class="mt-auto">
      <%= render "shared/footer" %>
    </footer>
  </body>
</html>
