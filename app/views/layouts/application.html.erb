<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Fasty App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# CSS は application.css（manifest）だけを読み込む %>
    <%# application.css 側で `*= require tailwind`, `*= require landing`, `*= require drawer` の順に指定 %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= yield :head %>

    <%# importmap を使う場合はこれだけでOK %>
    <%= javascript_importmap_tags %>

    <%# PWA / Icons %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
  </head>

  <%# ===== 背景画像の切り替えロジック ===== %>
  <% landing    = (controller_path == "pages" && action_name == "home") %>
  <% default_bg = asset_path("landing/hero.jpg") %>        <%# 未ログイン（トップ用） %>
  <% authed_bg  = asset_path("landing/hero-authed.png") %> <%# ログイン後用  %>

  <%# ページ側で content_for(:hero_bg_url) があれば最優先 %>
  <% page_override = content_for?(:hero_bg_url) ? yield(:hero_bg_url) : nil %>

  <%# ルート（未ログイン）またはログイン後の画面で背景を表示。
     さらに page_override があればそのページでも表示。 %>
  <% show_hero = landing || user_signed_in? || page_override.present? %>
  <% bg_url    = page_override || (user_signed_in? ? authed_bg : default_bg) %>

  <body
    data-controller="menu"
    class="min-h-dvh flex flex-col text-gray-900 <%= 'hero-bg' if show_hero %>"
    style="<%= show_hero ? "--hero-bg: url('#{bg_url}')" : '' %>">

    <%= render "shared/header" %>

    <%# ログイン後のみドロワー %>
    <%= render("shared/menu_drawer") if user_signed_in? %>

    <%# 画面高に追従するメイン。grow で余白を占有し、footer を下に押す %>
    <main class="grow">
      <div class="mx-auto max-w-5xl px-4">
        <%= render "shared/flash" %>
      </div>
      <%= yield %>
    </main>

    <%# mt-auto でフッターを最下部へ %>
    <footer class="mt-auto">
      <%= render "shared/footer" %>
    </footer>
  </body>
</html>
