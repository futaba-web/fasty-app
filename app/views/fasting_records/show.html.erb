<div class="max-w-3xl mx-auto p-4 space-y-2">
  <h1 class="text-xl font-bold">記録 #<%= @record.id %></h1>

  <p>開始: <%= l @record.start_time %></p>
  <p>終了: <%= @record.end_time ? l(@record.end_time) : "-" %></p>
  <p>目標: <%= @record.target_hours || "-" %> h</p>
  <p>結果: <%= @record.success.nil? ? "-" : (@record.success ? "達成" : "失敗") %></p>
  <p>コメント:<br><%= simple_format(@record.comment) %></p>

  <div class="pt-2">
    <%= link_to "一覧へ", fasting_records_path, class: "text-blue-600 underline" %> /
    <%= link_to "編集", edit_fasting_record_path(@record), class: "text-blue-600 underline" %>
  </div>
</div>
